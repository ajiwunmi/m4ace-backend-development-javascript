// 1. Function to calculate the sum of two numbers
function sum(a, b) {
	return a + b;
}

const a = 3;
const b = 9;
const total = sum(a, b);  
console.log(`Sum of ${a} and ${b} numbers is ${total}`); 
